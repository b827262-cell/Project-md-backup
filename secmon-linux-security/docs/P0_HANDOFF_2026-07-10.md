# GPT-5.6 Handoff — P0 / Issue #1

- Phase / Issue: P0 — 規格凍結與專案骨架 / `Project-md-backup#1`
- Implementation owner: GPT-5.6
- Independent verification owner: AGY

## Completed

- Added backend, frontend, database, migration, tests, ADR, and CI structure.
- Added typed environment settings with automatic blocking disabled by default.
- Added the initial SQLite migration, WAL setup, foreign keys, indexes, and
  migration tracking table.
- Added Python and frontend lint, type-check, test, build, and migration commands.

## Verification commands

Run from `secmon-linux-security/`:

```text
python -m pip install -e '.[dev]'
ruff check backend database tests
mypy backend database
pytest
npm --prefix frontend install
npm --prefix frontend run lint
npm --prefix frontend run typecheck
npm --prefix frontend run test
npm --prefix frontend run build
```

## Known limitations

- P0 intentionally contains no collectors, HTTP endpoints, authentication
  flow, threat engine, UI, or nftables commands; those belong to later phases.
- The frontend is a TypeScript command scaffold until P3 UI work begins.
- Dependency installation and CI execution require network access in a clean
  environment; AGY should run both jobs independently.

## AGY Independent Verification Report

- **Repository**: `get-rg`
- **Branch**: `main`
- **HEAD Commit**: `80874de36274d691aaf4cf23fc02063fd7fa4b57`
- **Verification Date**: 2026-07-10

### Modified Files List

1. `secmon-linux-security/database/migrate.py`: Enhanced to keep track of applied migrations in the `schema_migrations` table for proper idempotency.
2. `secmon-linux-security/tests/test_migrate.py`: Fixed long line (E501) lint error.
3. `secmon-linux-security/.env.example`: Added `SECMON_APP_NAME` to fully align with Pydantic configuration.
4. `.github/workflows/ci.yml`: Updated to run Python compile check and run the migration runner directly.
5. `secmon-linux-security/systemd/secmon-api.service`: Created secure systemd unit file with non-root execution and hardening flags.
6. `secmon-linux-security/systemd/secmon-collector.service`: Created secure systemd unit file with non-root execution and hardening flags.

### Verification Commands & Results

| Check Category | Command | Result |
| :--- | :--- | :--- |
| **Python Compilation** | `.venv/bin/python -m compileall -q backend database tests` | **PASS** (No errors/warnings) |
| **Python Lint** | `.venv/bin/ruff check backend database tests` | **PASS** (Fixed line-length error in test_migrate.py) |
| **Python Typecheck** | `.venv/bin/mypy backend database` | **PASS** (Success: no issues in 9 source files) |
| **Python Tests** | `.venv/bin/pytest` | **PASS** (2 tests passed) |
| **Frontend Lint** | `npm --prefix frontend run lint` | **PASS** (tsc --noEmit ran successfully) |
| **Frontend Typecheck** | `npm --prefix frontend run typecheck` | **PASS** (tsc --noEmit ran successfully) |
| **Frontend Tests** | `npm --prefix frontend run test` | **PASS** (1 test passed) |
| **Frontend Build** | `npm --prefix frontend run build` | **PASS** (tsc --noEmit ran successfully) |

### Migration & SQLite Verification

1. **Fresh Migration**: Running `.venv/bin/python database/migrate.py --database ./var/secmon.db` on a clean directory succeeded.
2. **Idempotency Check**: Running the migration command a second time completed successfully without modifying the schema or generating duplicate inserts.
3. **SQLite Validation**:
   - `PRAGMA quick_check;` output: `ok`
   - `PRAGMA foreign_key_check;` output: (empty - no violations)
   - Table and Index list verified: all 9 tables and 13 indexes match the schema.

### systemd hardening validation

- **Account**: Run as non-root user `secmon` / group `secmon`.
- **Restart Policy**: `Restart=on-failure`, `RestartSec=5s`, with throttling limits configured.
- **Hardening Flags**: `NoNewPrivileges=true`, `PrivateTmp=true`, `ProtectSystem=strict`, `ProtectHome=true`, `ReadWritePaths` restricted to specific app dirs.

### .env.example Validation

- Verified to contain no secrets, production API keys, or private IP configurations.
- Parameters map 1:1 with `backend/config.py`.

### GitHub Actions CI Configuration

- Verified `.github/workflows/ci.yml`. Added `compileall` and `migrate.py` execution to prevent regression and ensure full lint, typecheck, and test coverage in CI.

### Verification Status & Decision

- **Unfinished Items**: None.
- **Acceptance Decision**: **P0 正式驗收通過**
